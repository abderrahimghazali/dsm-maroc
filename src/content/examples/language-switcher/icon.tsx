import { LanguageSwitcher } from "@/dsm/components/language-switcher";

export default function LanguageSwitcherIcon() {
  return <LanguageSwitcher variant="icon" links={{ fr: "#fr", ar: "#ar", zgh: "#zgh", en: "#en" }} />;
}
